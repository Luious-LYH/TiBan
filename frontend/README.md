# 内镜智训Agent Frontend

React + Vite + TypeScript 前端工作台，包含首页总览、训练中心、错因分析、错误前提训练、报告草稿、科普卡片、模型看板、Skills 中心和审计日志。

```bash
npm install
npm run dev
npm run build
```

默认 API 地址为 `http://127.0.0.1:8000`，可通过 `VITE_API_BASE_URL` 覆盖。后端不可用时页面会显示本地 fallback 状态。

