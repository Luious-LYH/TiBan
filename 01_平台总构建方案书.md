# 内镜智训Agent：平台总构建方案书

> 项目名称：**内镜智训Agent：面向消化道内镜医师培训的智能辅导平台**  
> 用途：交给 Claude Code / Cursor / 其他 AI 编程 Agent 的平台开发总蓝图。  
> 当前阶段硬约束：**先完成可运行、可演示、可答辩的训练与智能辅导平台；真实模型评测流水线暂不开发，只保留“模型能力与风险定级”mock 看板和接口预留。**

---

## 1. 项目定位

### 1.1 一句话定位

内镜智训Agent 是面向消化道内镜医师培训的智能辅导平台，围绕题库分层、刷题训练、智能提示、答案讲解、错因反馈、报告草稿辅助和科普卡片生成形成完整训练闭环，并通过后台模型能力准入、风险标签和医生审核机制保障医疗教学场景中的安全边界。

### 1.2 必须保留 v1 的核心思想

v1 的思想不是材料包装，而是真实产品雏形：医师培训界面是主界面；诊断报告辅助、小卡片生成是可展示的应用拓展；后端应包含 skills 知识库、工具调用、多维度题库、模型库、小卡片模板库。v4 的价值是让标题和第一视觉中心更稳：内镜医师培训、智能辅导、可解释错因反馈。因此开发时必须同时保留 v1 的产品野心和 v4 的展示收敛。

### 1.3 当前版本目标

第一版平台要跑通以下核心功能：

1. 首页总览：今日训练、能力画像、推荐训练、安全提示。
2. 训练中心：内镜图像、病例摘要、题目、选项、提交答案。
3. 智能辅导：提示、追问、讲解、查看依据、下一题推荐。
4. 错因分析：根据答案和 atomic facts 给出细粒度反馈。
5. 错误前提训练：训练“证据不足 / 不适用 / 题干假设不成立”的意识。
6. 报告草稿辅助：根据医生输入的所见文本生成结构化草稿。
7. 科普卡片：根据医生审核后的结论生成患者友好解释卡片。
8. Skills 中心：展示和调用智能辅导、报告、科普、安全审查等技能。
9. Memory：记录当前训练会话、学员能力画像、错题、审计日志。
10. 模型库与能力看板：只做 mock/预留，不实现真实评测。
11. 审计管理：记录题库来源、模型版本、关键输出、医生审核状态。

### 1.4 当前版本非目标

| 非目标 | 处理方式 |
|---|---|
| 真实大规模模型评测流水线 | 暂不开发，只保留 mock 看板和接口预留 |
| 真实医院系统对接 | 不开发 |
| 真实患者数据导入 | 不开发，只使用脱敏样例或 mock 数据 |
| 独立临床诊断 | 禁止 |
| 完整权限/多租户系统 | 第一版只做角色 mock |
| 微信分享、小程序、打印生产流程 | 科普卡片只做预览，后续扩展 |

---

## 2. 总体架构

### 2.1 四层架构

```text
表现层 Frontend
  首页总览 / 训练中心 / 智能辅导 / 错因分析 / 错误前提训练
  报告草稿 / 科普卡片 / 模型库 / Skills 中心 / 审计管理

应用服务层 Backend
  题库服务 / 答题评分服务 / Agent 编排服务 / Skills 服务
  工具调用服务 / 记忆服务 / 报告服务 / 科普卡片服务 / 模型库服务 / 审计服务

Agent 层
  TrainingTutorAgent / ErrorAnalysisAgent / ReportDraftAgent
  PatientEducationAgent / SkillRouterAgent / SafetyGuardAgent / AuditAgent

数据层
  questions / submissions / learner_profiles / skills / model_registry
  report_templates / card_templates / audit_logs / memory_store
```

### 2.2 推荐技术栈

| 模块 | 技术 | 原因 |
|---|---|---|
| 前端 | React + Vite + TypeScript | 快速搭建、适合竞赛原型 |
| UI | Tailwind CSS + shadcn/ui 风格 + lucide-react | 专业、现代、AI 生成友好 |
| 图表 | Recharts | 能力雷达、趋势、错因分布 |
| 后端 | FastAPI + Pydantic | Python 生态，便于接 Agent/模型脚本 |
| 数据 | JSON mock 起步，SQLite 过渡 | 保证第一版稳定可演示 |
| Agent 编排 | 第一版手写工作流，后续 LangGraph | 不让框架复杂度拖慢开发 |
| LLM 接口 | OpenAI-compatible provider 抽象 | 支持本地模型/API 模型 |
| AI 编程 | Claude Code + Cursor + 人工验收 | 生成、审阅、修复多轮循环 |

---

## 3. 页面设计

### 3.1 页面优先级

| 优先级 | 页面 | 目标 |
|---|---|---|
| P0 | 首页总览 | 一眼看懂平台是内镜培训智能辅导系统 |
| P0 | 训练中心 | 核心演示：图像、题目、提交、反馈 |
| P0 | 智能辅导面板 | 核心演示：提示、追问、解释、下一题 |
| P0 | 错因分析 / 原子事实反馈 | 展示核心创新 |
| P1 | 错误前提训练 | 展示医疗安全意识 |
| P1 | 报告草稿辅助 | 展示医生审核前辅助 |
| P1 | 科普卡片 | 展示趣味性和社会价值 |
| P1 | 模型库与能力看板 | 展示后台安全机制，当前 mock |
| P2 | Skills 中心 | 展示可扩展 Agent 能力 |
| P2 | 审计管理 | 展示合规和可追踪 |

### 3.2 训练中心三栏布局

```text
左侧：内镜图像 / 病例摘要 / 题源标签
中间：题目 / 选项 / 提交按钮 / 练习或考试模式
右侧：智能辅导对话 / 提示 / 追问 / 查看依据 / 下一题
底部：复杂度、题型、教学标签、错因标签、能力变化
```

### 3.3 演示路径

1. 打开首页：展示今日训练、能力画像、推荐题型。
2. 进入训练中心：展示图像和题目。
3. 点击“提示一下”：智能辅导只给方向，不直接泄露答案。
4. 提交一个错误答案：系统评分。
5. 查看错因分析：展示 atomic facts、错误类型、学习建议。
6. 切换错误前提题：展示“证据不足/不适用”。
7. 生成报告草稿：显示医生审核标识。
8. 生成科普卡片：显示患者友好解释和免责声明。
9. 打开模型能力看板：说明这是后台安全准入 mock。
10. 打开审计日志：看到关键事件被记录。

---

## 4. Agent 设计

### 4.1 总原则

本平台中的 Agent 不是自由聊天机器人，而是受控的教学工作流。Agent 必须依赖题目、标准答案、atomic facts、用户历史、skills 和安全规则生成反馈。

### 4.2 Agent 列表

| Agent | 阶段 | 作用 |
|---|---|---|
| TrainingTutorAgent | P0 | 刷题提示、追问、讲解、下一题推荐 |
| ErrorAnalysisAgent | P0 | 根据答案和 atomic_trace 生成错因分析 |
| SafetyGuardAgent | P0 | 检查输出是否越过医疗边界 |
| ReportDraftAgent | P1 | 根据医生输入生成结构化报告草稿 |
| PatientEducationAgent | P1 | 生成患者科普卡片草稿 |
| SkillRouterAgent | P1 | 根据用户意图选择 skill/tool |
| AuditAgent | P2 | 记录模型、题库、输出、审核状态 |
| ModelSelectorAgent | P2 | 根据模型标签选择默认模型，当前 mock |

### 4.3 核心工作流

#### 刷题辅导工作流

```text
加载题目 -> 请求提示 -> TrainingTutorAgent 给提示 -> 用户提交答案
-> grade_answer 工具评分 -> ErrorAnalysisAgent 分析错因
-> SafetyGuardAgent 审查输出 -> 更新 learner_profile
-> 推荐下一题 -> 写入 audit_logs
```

#### 报告草稿工作流

```text
医生输入所见文本 -> ReportDraftAgent 结构化字段
-> SafetyGuardAgent 检查过度诊断风险
-> 输出草稿 + 复核点 + doctor_review_required=true
-> 写入 audit_logs
```

#### 科普卡片工作流

```text
输入医生审核后的诊断或教学样例 -> PatientEducationAgent 通俗化
-> SafetyGuardAgent 检查诊疗承诺风险
-> 渲染卡片模板 -> 写入 audit_logs
```

---

## 5. Skills、Tools、Memory

### 5.1 Skills 设计

| Skill | 作用 |
|---|---|
| question_hint | 根据题目给提示，不泄露答案 |
| answer_explain | 解释参考答案和关键依据 |
| atomic_feedback | 生成原子事实级反馈 |
| false_premise_guard | 判断题干假设不成立、证据不足、不适用 |
| next_question | 推荐下一题型 |
| report_structure | 报告草稿结构化 |
| patient_card | 科普卡片生成 |
| safety_review | 医疗安全审查 |
| audit_log | 审计日志记录 |

### 5.2 Tools 设计

| Tool | 当前实现 |
|---|---|
| get_question / list_questions | JSON/SQLite 查询 |
| grade_answer | 规则匹配 |
| atomic_fact_audit | 根据 atomic_trace 规则生成 |
| false_premise_check | 根据 false_premise_flag 和答案判断 |
| update_learner_profile | SQLite/JSON 更新 |
| recommend_next_question | 规则推荐 |
| generate_tutor_reply | 模板，后续接 LLM |
| generate_report_draft | 模板，后续接 LLM |
| generate_patient_card | 模板，后续接 LLM |
| safety_review | 规则 + 模板 |
| log_event | 审计日志 |

### 5.3 Memory 设计

| Memory | 存储内容 | 第一版实现 |
|---|---|---|
| Session Memory | 当前题目、对话、答题状态 | 前端 state + 后端 session_id |
| Learner Memory | 错题、薄弱项、能力分 | learner_profile JSON/SQLite |
| Tutor Memory | 最近提示与讲解 | tutor_messages |
| Safety Memory | 安全提醒、审核状态 | audit_logs |
| Model Memory | 当前模型和风险标签 | model_registry |

---

## 6. 后端设计

推荐目录：

```text
backend/
  main.py
  app/
    core/config.py
    core/safety.py
    routers/questions.py
    routers/submissions.py
    routers/tutor.py
    routers/reports.py
    routers/cards.py
    routers/models.py
    routers/skills.py
    routers/audit.py
    services/question_service.py
    services/grading_service.py
    services/tutor_orchestrator.py
    services/skill_registry.py
    services/memory_service.py
    services/safety_service.py
    schemas/*.py
    data/*.json
```

核心 API：

```text
GET  /api/health
GET  /api/dashboard
GET  /api/questions
GET  /api/questions/{id}
POST /api/submit
POST /api/tutor/hint
POST /api/tutor/explain
POST /api/tutor/chat
GET  /api/learner/profile
GET  /api/learner/recommendations
POST /api/report-draft
POST /api/patient-card
GET  /api/models
POST /api/models/select
GET  /api/skills
POST /api/skills/run
GET  /api/audit
```

所有医疗相关输出必须包含：

```json
{
  "doctor_review_required": true,
  "safety_notice": "仅供教学训练或医生审核前辅助，不作为独立诊断依据。"
}
```

---

## 7. 前端设计

推荐目录：

```text
frontend/src/
  pages/Dashboard.tsx
  pages/TrainingCenter.tsx
  pages/ErrorFeedback.tsx
  pages/FalsePremiseTraining.tsx
  pages/ReportDraft.tsx
  pages/PatientCard.tsx
  pages/ModelHub.tsx
  pages/SkillsCenter.tsx
  pages/AuditPanel.tsx
  components/layout
  components/training
  components/tutor
  components/feedback
  components/report
  components/card
  components/model
  lib/api.ts
  lib/types.ts
```

视觉风格：医疗教学系统、白底或浅灰底、蓝绿主色、橙色用于风险提示、卡片式布局、信息密度高但不拥挤。

---

## 8. 里程碑

| 里程碑 | 交付 |
|---|---|
| M0 仓库初始化 | 前后端目录、README、AGENTS.md、.env.example |
| M1 前端静态原型 | 首页、训练中心、错因反馈、报告草稿、科普卡片、模型看板 |
| M2 后端 mock API | FastAPI 和所有核心接口 |
| M3 Agent 工作流 | Tutor、ErrorAnalysis、Safety、Report、Card、Skills、Memory |
| M4 前后端联调 | API 调用、fallback、审计日志、能力画像更新 |
| M5 演示打磨 | 演示数据、截图、录屏、DEMO_SCRIPT、答辩话术 |

---

## 9. 最终验收标准

- 前端可启动，后端可启动。
- 训练中心能完成一次完整答题流程。
- 智能辅导能给提示和讲解。
- 错因反馈能展示 atomic facts。
- 错误前提题能展示证据不足/不适用逻辑。
- 报告草稿和科普卡片均有医生审核提示。
- 模型能力看板明确为 mock/预留。
- 审计日志能记录至少 5 类事件。
- 不包含真实密钥、真实患者身份、服务器敏感信息。
- README、API_SPEC、DEMO_SCRIPT、AGENTS.md 完整。
