# 系统需求规格与接口数据字典

> 用途：统一前端、后端、Agent、mock 数据和验收标准。

---

## 1. 角色与场景

| 角色 | 当前实现 | 使用场景 |
|---|---|---|
| learner | mock 用户 | 刷题、请求提示、提交答案、查看错因 |
| teacher | mock 角色 | 查看学员画像、题库和审计记录 |
| doctor | mock 角色 | 使用并审核报告草稿、科普草稿 |
| admin | mock 角色 | 管理模型库、skills、日志 |

核心用户故事：学员完成一次内镜训练题，智能辅导给提示，提交后系统解释答案并给出原子事实级错因反馈；医生可以输入所见文本生成报告草稿；系统记录关键输出和医生审核状态。

---

## 2. 数据实体

### 2.1 Question

```ts
type Question = {
  id: string
  title: string
  image_url?: string
  image_placeholder: string
  case_summary: string
  question: string
  options: string[]
  answer: string
  explanation: string
  complexity: 1 | 2 | 3
  question_class: "基础识别" | "部位定位" | "病变属性" | "复杂组合" | "错误前提" | "报告纠错" | "一图多问"
  source_type: "公开基础问答" | "公开复杂问答" | "公开综合基准" | "医院合作中文病例" | "教学样例"
  atomic_trace: AtomicFact[]
  false_premise_flag: boolean
  teaching_tags: string[]
  difficulty: "入门" | "进阶" | "挑战"
  doctor_review_required: boolean
  safety_notice: string
}
```

### 2.2 AtomicFact

```ts
type AtomicFact = {
  id: string
  fact: string
  expected: string
  supported: boolean
  evidence: string
  skill_dimension: "病灶识别" | "部位定位" | "属性判断" | "数量判断" | "事实组合" | "证据不足识别"
}
```

### 2.3 Submission

```ts
type Submission = {
  id: string
  question_id: string
  learner_id: string
  selected_answer: string
  is_correct: boolean
  score: number
  error_tags: string[]
  fact_feedback: AtomicFact[]
  explanation: string
  next_recommendation: string
  created_at: string
}
```

### 2.4 LearnerProfile

```ts
type LearnerProfile = {
  learner_id: string
  name: string
  total_questions: number
  accuracy: number
  skill_scores: Record<string, number>
  weakness_tags: string[]
  recent_errors: string[]
  recommended_question_classes: string[]
  updated_at: string
}
```

### 2.5 SkillDefinition

```ts
type SkillDefinition = {
  id: string
  name: string
  description: string
  category: "training" | "feedback" | "report" | "card" | "safety" | "audit"
  enabled: boolean
  risk_level: "low" | "medium" | "high"
  input_schema: Record<string, unknown>
  output_schema: Record<string, unknown>
}
```

### 2.6 ReportDraft

```ts
type ReportDraft = {
  id: string
  input_finding_text: string
  exam_type: string
  structured_findings: string[]
  draft_impression: string[]
  review_points: string[]
  uncertainty_notes: string[]
  doctor_review_required: true
  safety_notice: string
  created_at: string
}
```

### 2.7 PatientCard

```ts
type PatientCard = {
  id: string
  card_title: string
  plain_language_explanation: string
  what_it_means: string[]
  what_to_watch: string[]
  follow_up_reminder: string
  disclaimer: string
  doctor_review_required: true
  safety_notice: string
  created_at: string
}
```

### 2.8 ModelProfile

```ts
type ModelProfile = {
  id: string
  name: string
  provider_type: "local" | "api" | "mock"
  model_family: "通用多模态" | "医学多模态" | "内镜领域" | "闭源API"
  recommended_roles: string[]
  risk_tags: string[]
  ability_scores: {
    basic_recognition: number
    complex_reasoning: number
    false_premise: number
    chinese_report: number
    engineering: number
  }
  grade: "S" | "A" | "B" | "C"
  is_active: boolean
}
```

### 2.9 AuditLog

```ts
type AuditLog = {
  id: string
  event_type: "question_view" | "answer_submit" | "tutor_reply" | "report_draft" | "patient_card" | "skill_run" | "model_select" | "safety_warning"
  user_id: string
  entity_id?: string
  summary: string
  risk_level: "low" | "medium" | "high"
  doctor_review_required: boolean
  created_at: string
}
```

---

## 3. API 规格

### 3.1 GET /api/health

```json
{ "status": "ok", "service": "endo-zhixun-agent-backend" }
```

### 3.2 GET /api/dashboard

返回今日训练数、近期正确率、薄弱标签、推荐训练、当前模型、安全提示。

### 3.3 GET /api/questions

Query：`question_class?`、`difficulty?`、`false_premise?`。

Response：

```json
{ "items": [], "total": 24 }
```

### 3.4 GET /api/questions/{id}

Response：

```json
{ "item": { "id": "q001" } }
```

### 3.5 POST /api/submit

Request：

```json
{ "question_id": "q001", "learner_id": "demo_learner", "selected_answer": "存在" }
```

Response：

```json
{
  "is_correct": true,
  "score": 100,
  "error_tags": [],
  "fact_feedback": [],
  "explanation": "回答正确。",
  "next_recommendation": "建议继续练习复杂组合题。",
  "safety_notice": "仅供教学训练或医生审核前辅助，不作为独立诊断依据。"
}
```

### 3.6 POST /api/tutor/hint

Response 必须包含 `hint`、`leak_answer=false`、`safety_notice`。

### 3.7 POST /api/tutor/explain

Response 必须包含 `explanation`、`error_tags`、`atomic_feedback`、`next_recommendation`、`safety_notice`。

### 3.8 POST /api/report-draft

Request：

```json
{ "finding_text": "胃窦黏膜充血，可见散在糜烂。", "exam_type": "gastroscopy" }
```

Response：必须包含 `structured_findings`、`draft_impression`、`review_points`、`uncertainty_notes`、`doctor_review_required=true`、`safety_notice`。

### 3.9 POST /api/patient-card

Response：必须包含 `card_title`、`plain_language_explanation`、`what_it_means`、`what_to_watch`、`follow_up_reminder`、`disclaimer`、`doctor_review_required=true`、`safety_notice`。

### 3.10 GET /api/models

返回模型库和 mock 能力分，不得声称真实评测已完成。

### 3.11 GET /api/audit

返回审计日志列表。

---

## 4. 页面验收标准

| 页面 | 验收标准 |
|---|---|
| Dashboard | 项目名、今日训练、能力画像、推荐训练、安全提示 |
| TrainingCenter | 加载题目、请求提示、选择答案、提交结果、进入错因分析 |
| ErrorFeedback | 展示 atomic_trace、错误标签、学习建议、下一题推荐 |
| FalsePremiseTraining | 能展示证据不足/不适用逻辑 |
| ReportDraft | 输入所见文本，生成结构化草稿和医生审核提示 |
| PatientCard | 输入诊断要点，生成卡片预览和免责声明 |
| ModelHub | 展示模型库、风险标签、当前模型，标注 mock/预留 |
| SkillsCenter | 展示 skills，可运行至少 3 个 skill |
| AuditPanel | 展示 event_type、summary、risk_level、doctor_review_required |

---

## 5. 不可违反的安全规则

1. 不替代医生诊断。
2. 不输出最终临床结论。
3. 不处理真实患者身份信息。
4. 不写真实密钥。
5. 不写服务器敏感信息。
6. 医疗输出必须有 safety_notice。
7. 报告和科普必须有 doctor_review_required=true。
8. 错误前提题必须允许“证据不足/不适用”。
9. 模型能力看板不得声称完成真实临床评测。
10. 演示数据必须是脱敏样例或 mock。
