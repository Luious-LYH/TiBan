# 模型算法线环境门禁

- 执行日期：2026-08-20
- 主机标识：`84-proxy`（仓库文件不保存真实地址）
- GPU：8 × NVIDIA GeForce RTX 4090，单卡 24,564 MiB
- 门禁时显存：8 张卡均为 1 MiB，满足空闲阈值 `< 500 MiB`
- 备用主机：`89-proxy`，3 × NVIDIA GeForce RTX 3090，门禁时均为 1 MiB
- 隔离目录：`~/aris_portfolio_runs/20260820`
- 运行隔离：用户目录虚拟环境 + 单独 GNU Screen 会话，不修改公共环境
- 数据：7 张项目已收录的公开 Kvasir-VQA-x1 / EndoBench 教学样例
- 合规口径：仅为作品集工程基准，不是临床验证，不输出临床有效性结论

## 环境恢复记录

系统 Python 缺少 `ensurepip`，因此标准 `python -m venv` 不可用。实验改为用户目录的 `uv` 创建隔离环境；这不会安装系统包，也不影响服务器上的其他任务。完整安装与运行日志在本目录随结果一并保存。

## 时间盒

若 14:00 前 3B 模型仍不能完成推理，则停止环境重试，交付可复现脚本、公开样例清单和本门禁报告；QLoRA 仅在推理基准成功后进行 smoke 验证。

## 门禁结论

13:54 完成依赖导入与 CUDA/BF16 检查，13:57 完成 7 例推理，13:58 完成 1-step QLoRA smoke；随后按总负责人追加要求完成 7 例、10-step 训练集 sanity。主机归还时 8 张 GPU 均为 1 MiB 占用，未遗留运行会话。
